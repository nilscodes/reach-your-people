# We do not want to generate the following files because we are not making this a complete npm package, and instead just use the raw outputs from the openapi-generator-cli as an internal library.

.openapi-generator
.gitignore
.npmignore
git_push.sh
package.json
README.md
tsconfig.json
